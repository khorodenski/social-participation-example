import StagePlaceholder from '../components/StagePlaceholder';
import { pl } from '../i18n/pl';

/** Milestone 5 — three image cards with retry. */
export default function VisualizeStage() {
  return <StagePlaceholder title={pl.stages.visualizing} />;
}
